{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="{{ asset('assets/css/grid-list.css') }}">
{% endblock %}

{% block title %}Planters{% endblock %}

{% block body %}
<div class="content-wrapper">

    <h1>Planters</h1>

    <div class="block-wrapper">
    <p>Click on a button to choose list view or grid view.</p>

    <div id="btnContainer">
        <button class="btn btn-custom" onclick="listView()"><i class="fa fa-bars"></i> List</button>
        <button class="btn btn-custom active" onclick="gridView()"><i class="fa fa-th-large"></i> Grid</button>
    </div>
    <br>

    <div class="row">
        {% for p in planters %}
            <a class="no-styling text-black" href="{{ path('planter_detail' , {id: p.id}) }}">
                <div class="column {{ p.color }}" >
                    <h2>{{ p.getName() }}</h2>
                    <img width="30px" height="30px" src="{{ asset('assets/images/water-drop-blue.svg') }}"  alt=""/>
                    <p>plant: <i>{{ presets[p.getPlantPresetsId].name }}</i></p>
                    <a href="{{ path('planter_edit' , {id: p.getId}) }}" class="btn btn-dark btn-sm">editovat nastavení</a>
                </div>
            </a>
        {% endfor %}
    </div>
        <a href="{{ path('planter_new') }}" class="btn btn-primary btn-block">add new</a>
    </div>
    <script type="text/javascript" src="{{ asset('assets/js/grid-list.js') }}"></script>
</div>
{% endblock %}
