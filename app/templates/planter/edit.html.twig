{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}


{% block body %}

    <div class="example-wrapper">
    <h1>Planter: {{ planterName }}</h1>

    {{ form_start(PlanterForm) }}
        {{ form_widget(PlanterForm.name) }}
        {{ form_widget(PlanterForm.plant_presets_id) }}
        {{ form_widget(PlanterForm.color) }}
        {{ form_widget(PlanterForm.save) }}
        {{ form_widget(PlanterForm.delete) }}
    {{ form_end(PlanterForm) }}

    </div>


{% endblock %}